// Holdfast signaling server.
// Implementation lands in task #3 — peer registry + ICE candidate broker over WebSocket.

import { PROTOCOL_VERSION } from '@holdfast/protocol';

console.log(`holdfast signaling server (protocol v${PROTOCOL_VERSION}) — not yet implemented`);
